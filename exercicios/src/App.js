import React from 'react';
import { Text, SafeAreaView, View } from 'react-native';

export default () => {
    return (
        <SafeAreaView>
            <View>
                <Text>Sem Título</Text>
            </View>
        </SafeAreaView>
    );
}